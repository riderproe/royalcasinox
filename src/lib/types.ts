export type Game = {
    title: string;
    slug: string;
    genre: string;
    platform: string;  // e.g., "Android / iOS"
    image: string;     // URL or /assets local
    link: string;      // official download/store URL
    description: string;
  };
  